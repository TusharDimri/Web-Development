<%- include("partials/header"); -%>

<h1>Home</h1>

<p><%= startingContent %></p>

<% posts.forEach((post)=>{ %>
    <!-- <% post_content = `${post["post"]}`%> -->
    <!-- <% post_content = `${post_content.slice(0, post_content.length/4)}...` %> -->
    <h1><%= post["title"] %></h1>
    <p><%= post["post"].substring(0, 100) + "..." %><a href='/posts/<%=post["title"]%>'>Read More</a></p>
<% }); %>

<%- include("partials/footer"); -%>